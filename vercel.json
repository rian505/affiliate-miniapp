{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(api|generate-prompt|sheets|sheets-read|products)(.*)", "dest": "app.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
